<!--
	
Title: Add New User | Sutro CMS
Description: This view provides the actual form to add a new user, specifying:
	- Full name
	- Email
	- Password
	- Password confirmation (user will be able to change later)
	- Permission level

-->

<div class="wrapper">
	<h2>Add New User</h2>
		
	<%= form_for @user do |f| %>
		<!-- 
		- Display errors, if any. (outside form?)
		- Need fields for
		-- :email
		-- :password
		-- :password_confirmation
		-- f.submit			
		-->
		
		<%= f.label :full_name %>
		<%= f.text_field :full_name %>
		
		<%= f.label :email %>
		<%= f.text_field :email %>
		
		<%= f.label :password %>
		<%= f.text_field :password %>
		
		<%= f.label :password_confirmation %>
		<%= f.text_field :password_confirmation %>
		
		<%= f.submit "Add User", :class => "cta submit" %>	
		
	<% end %>
</div>