<% if @user.errors.any? %>
  <div class="flash flash-error">
    <h2>Invalid Submission</h2>
    <ul>
      <% for message in @user.errors.full_messages %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>  
<% end %>